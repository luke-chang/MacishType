# Build-dependency globs for GenerateSymbolNames.swift, one per line,
# relative to Scripts/. HandleExternalResources folds each matching file's
# content into the processor's version hash, so editing any of them
# regenerates the processor's outputs. Blank lines and #-comments are ignored.
SymbolNamesOverride.*.txt
